# demo/

Drop the pinned demo PDF here as `clf-c02-exam-guide.pdf`.

The binary is intentionally **not committed** (kept out of git via `.gitignore`). See
[`../docs/demo.md`](../docs/demo.md) for the pinned source and the cold demo click-path.
